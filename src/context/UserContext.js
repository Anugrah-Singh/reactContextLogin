import React from "react";

const UserContext = React.createContext()

export default UserContext;

//We are exporting the user context variable which stores context of the data because we
//  have used create context react method